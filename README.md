# Blazor.SSR

This repository extracts the static SSR enhancement portions of ASP.NET Core Blazor Web.JS into a standalone SSR-only JavaScript runtime.

## Toolchain

See [docs/toolchain.md](docs/toolchain.md) for public npm install, build, test, and lint instructions.
